**Unreleased**
* Enabled TLS certificate verification by default for Symantec Security Analytics assets.
* Kept downloaded PCAP filenames within the vault temporary directory.
* Rejected failed HTTP PCAP downloads before writing them to the vault.
